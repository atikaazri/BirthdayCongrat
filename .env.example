# BDVoucher Configuration
# Copy this file to .env and update the values for your deployment

# ===========================================
# BASIC CAFE SETTINGS
# ===========================================
CAFE_NAME=Hey Hey Cafe
CAFE_LOCATION=Muscat, Oman
VOUCHER_REWARD=FREE Drink
VOUCHER_VALUE=$15

# ===========================================
# VOUCHER EXPIRY SETTINGS (Choose one mode)
# ===========================================
# Option 1: Set expiry in hours (recommended for short-term vouchers)
VOUCHER_EXPIRY_MODE=hours
VOUCHER_VALIDITY_HOURS=24

# Option 2: Set expiry in days (comment out hours mode above to use this)
# VOUCHER_EXPIRY_MODE=days
# VOUCHER_VALIDITY_DAYS=1

# ===========================================
# WHATSAPP MESSAGING SETTINGS
# ===========================================
MESSAGING_SERVICE=ultramsg
TEXTMEBOT_KEY=your_text_me_bot_api_key
ULTRAMSG_INSTANCE_ID=instance_id      #your_instance_id_here
ULTRAMSG_TOKEN=token  #your_token_here

# ===========================================
# AUTOMATIC MESSAGING SETTINGS
# ===========================================
# Enable/disable automatic birthday messaging
AUTO_MESSAGING_ENABLED=True

# Time to send birthday messages (24-hour format: HH:MM)
AUTO_MESSAGING_TIME=09:00

# Timezone for scheduling (use standard timezone names)
AUTO_MESSAGING_TIMEZONE=Asia/Muscat

# ===========================================
# CUSTOM BIRTHDAY MESSAGE TEMPLATE
# ===========================================
# Available placeholders: {employee_name}, {cafe_name}, {cafe_location}, 
# {voucher_reward}, {voucher_value}, {validity_period}
BIRTHDAY_MESSAGE_TEMPLATE=Happy Birthday {employee_name}!

From all of us at {cafe_name}, we hope you have a wonderful day! As a special birthday treat, here's a voucher for a {voucher_reward} worth {voucher_value}.

Location: {cafe_location}
Valid for: {validity_period}

Enjoy your special day!

# ===========================================
# SERVER SETTINGS
# ===========================================
# Main application port
PORT=5000

# Cafe interface port (for staff to redeem vouchers)
CAFE_PORT=5001

# Admin interface port (for management dashboard)
ADMIN_PORT=5002

# Debug mode (set to False for production)
DEBUG=False

# ===========================================
# SECURITY & ENCRYPTION SETTINGS
# ===========================================

# Derived key from password + salt (used for Fernet encryption)
# ⚠️ Change these before deployment!
ENCRYPTION_PASSWORD=super-strong-password-change-me

# Generate this with: python -c "import os, base64; print(base64.urlsafe_b64encode(os.urandom(16)).decode())"
ENCRYPTION_SALT=p6kAfs4qCwE_7fEPvbcP7A==

# Optional: set a full Fernet key instead of password+salt (base64 32-byte key)
# ENCRYPTION_KEY=

# Flask secret key for session signing and CSRF protection
FLASK_SECRET_KEY=change-this-secret-key-in-production-12345

# JWT secret for authentication tokens
JWT_SECRET_KEY=your-jwt-secret-key-change-me

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# 1. Update ULTRAMSG_INSTANCE_ID and ULTRAMSG_TOKEN with your actual credentials
# 2. Set AUTO_MESSAGING_TIME to your preferred time (e.g., 08:30, 10:00, etc.)
# 3. Choose voucher expiry mode: 'hours' for short-term (24h) or 'days' for longer (1-7 days)
# 4. Customize BIRTHDAY_MESSAGE_TEMPLATE to match your cafe's style
# 5. Set DEBUG=False for production deployment
# 6. The system will automatically send birthday messages at the configured time daily
